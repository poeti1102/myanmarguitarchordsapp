import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.winxstudio.mmguitarchords',
  appName: 'MyanmarGuitarChords',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
